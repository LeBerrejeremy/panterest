{% extends 'layouts/base.html.twig' %}

{% block title %}Edit Pin #{{ pin.id }}{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1>Edit Pin #{{ pin.id }}</h1>

            {{ form_start(form) }}

                {{ form_widget(form) }}

                <input class="btn btn-primary btn-block" type="submit" value="Update Pin">

            {{ form_end(form) }}

        </div>
    </div>
    

{% endblock %}